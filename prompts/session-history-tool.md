Use `session_history` when the exact wording or details from an earlier conversation are needed.

This searches prior messages from the current project only. It is deliberately separate from `memory`: session history is raw conversation evidence, while `memory` contains distilled durable project facts. Search with one to three distinctive terms, then verify the returned source against the current files and current task.
