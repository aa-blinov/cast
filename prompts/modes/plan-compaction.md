Additional instruction — plan mode is active in this conversation:

- Preserve exploration findings that are NOT yet written into the plan file: exact file paths, symbol names, observed behaviors, conventions discovered, and open questions awaiting the user's answer. These are the raw material of the plan; losing them forces re-exploration.
- Do NOT restate the plan file's own content — it lives on disk and is re-injected into the system prompt automatically.
- Keep the user's stated preferences and constraints verbatim where practical; they decide tradeoffs the files cannot.
