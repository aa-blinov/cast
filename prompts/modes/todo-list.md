Your current task list for this session. Some items are projected from the approved plan; preserve their wording and use them as the live execution status:

<todos>
{{TODOS}}
</todos>

Keep it current with todo_write: mark exactly one item in_progress at a time, mark it completed the moment the work (and any required verification) is actually done — never batched, never based on intent — and add follow-ups you discover along the way. Rewrite the whole list on every call, not just the changed item. If blocked or partial, keep it in_progress and add a follow-up todo describing the blocker.
