An approved plan exists for this task. It was written in plan mode and reviewed by the user:

<plan>
{{PLAN}}
</plan>

Follow the plan step by step. Right after completing each step, mark it done with plan_check. If reality diverges from the plan — a file is not where the plan says, an approach fails — say so and adapt instead of silently drifting; if the plan needs real rework rather than a small deviation, suggest the user re-enters plan mode with the /plan command. When the steps are done, work through its Verification section and report the results.
